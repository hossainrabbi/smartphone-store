import { Container, Grid } from '@material-ui/core';
import React from 'react';

const Products = () => {
    return (
        <div>
            <Container>
                <Grid container></Grid>
            </Container>
        </div>
    );
};

export default Products;
